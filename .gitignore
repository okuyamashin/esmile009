# OS / IDE
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp
*.swo

# 環境・鍵（公開リポジトリではコミットしない）
.env
.env.*
!.env.example
*.pem
*.key

# 個人情報・社外秘になりうる入出力（テストはローカルのみ）
samples/in/*.xlsx
samples/in/*.pdf
samples/in/*.xls
samples/in/~$*
samples/out/*.pdf

# アクセスログ（ローカル / サーバー）
logs/*.log

# 変換 PDF 保存（ローカル / サーバー）
data/convert/*.pdf
data/convert/*.xlsx
data/convert/*.xls

# 非同期ジョブ（ローカルキュー・入出力）
data/jobs/**/*
!data/jobs/.gitkeep

# Python
__pycache__/
*.py[cod]
.venv/
venv/

# ビルド成果物
dist/
